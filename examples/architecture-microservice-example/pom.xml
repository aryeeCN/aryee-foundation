<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>cn.aryee.foundation</groupId>
    <artifactId>architecture-microservice-example</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>Aryee Examples - Architecture Microservice</name>
    <description>
        微服务架构形态示例 —— 演示 bom-full 版本管理 + 按需组合模块 Starter 开箱即用。
        Nacos 注册中心 + Nacos 配置中心 + Seata 分布式事务 + XXL-Job 分布式调度 + Redis 缓存。
    </description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <spring.boot.version>4.0.7</spring.boot.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>cn.aryee.foundation</groupId>
                <artifactId>bom-full</artifactId>
                <version>1.0.0-SNAPSHOT</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- ========== 微服务架构推荐模块组合（bom-full 管版本，此处无需写 version） ========== -->

        <!-- Spring 公共模块 -->
        <dependency>
            <groupId>cn.aryee.foundation</groupId>
            <artifactId>commons-spring</artifactId>
        </dependency>

        <!-- 缓存模块 -->
        <dependency>
            <groupId>cn.aryee.foundation</groupId>
            <artifactId>cache-spring-boot-starter</artifactId>
        </dependency>

        <!-- 数据库模块 -->
        <dependency>
            <groupId>cn.aryee.foundation</groupId>
            <artifactId>database-spring-boot-starter</artifactId>
        </dependency>

        <!-- 存储模块 -->
        <dependency>
            <groupId>cn.aryee.foundation</groupId>
            <artifactId>storage-spring-boot-starter</artifactId>
        </dependency>

        <!-- 事件模块 -->
        <dependency>
            <groupId>cn.aryee.foundation</groupId>
            <artifactId>event-spring-boot-starter</artifactId>
        </dependency>

        <!-- 调度模块 -->
        <dependency>
            <groupId>cn.aryee.foundation</groupId>
            <artifactId>scheduler-spring-boot-starter</artifactId>
        </dependency>

        <!-- 安全模块 -->
        <dependency>
            <groupId>cn.aryee.foundation</groupId>
            <artifactId>security-spring-boot-starter</artifactId>
        </dependency>

        <!-- 通信模块 -->
        <dependency>
            <groupId>cn.aryee.foundation</groupId>
            <artifactId>transport-spring-boot-starter</artifactId>
        </dependency>

        <!-- 事务模块 -->
        <dependency>
            <groupId>cn.aryee.foundation</groupId>
            <artifactId>transaction-spring-boot-starter</artifactId>
        </dependency>

        <!-- Seata 分布式事务（transaction-infrastructure 中为 optional，微服务工程需显式引入） -->
        <dependency>
            <groupId>org.apache.seata</groupId>
            <artifactId>seata-spring-boot-starter</artifactId>
        </dependency>

        <!-- 监控模块 -->
        <dependency>
            <groupId>cn.aryee.foundation</groupId>
            <artifactId>monitoring-spring-boot-starter</artifactId>
        </dependency>

        <!-- 网关模块 -->
        <dependency>
            <groupId>cn.aryee.foundation</groupId>
            <artifactId>gateway-spring-boot-starter</artifactId>
        </dependency>

        <!-- ========== Spring Boot & 第三方 ========== -->

        <!-- Spring Boot Web (Blocking/WebMVC 场景) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Spring Data JPA -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- Spring Data Redis（缓存模块 redis 实现需要 RedisTemplate 在 classpath） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>

        <!-- MySQL 驱动（微服务架构常用外部数据库） -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- Spring Boot Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>21</release>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${spring.boot.version}</version>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>