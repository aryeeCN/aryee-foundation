<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>cn.aryee.foundation</groupId>
    <artifactId>architecture-cloudnative-example</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>Aryee Examples - Architecture Cloud Native</name>
    <description>
        云原生架构形态示例 —— 演示 bom-full 版本管理 + 按需组合模块 Starter 开箱即用。
        K8s ConfigMap 配置热刷新 + Service Mesh 流量治理 + OpenTelemetry 监控 + 对象存储。
    </description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <spring.boot.version>4.0.7</spring.boot.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>cn.aryee.foundation</groupId>
                <artifactId>bom-full</artifactId>
                <version>1.0.0-SNAPSHOT</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- ========== 云原生架构推荐模块组合（bom-full 管版本，此处无需写 version） ========== -->

        <!-- Spring 公共模块 -->
        <dependency>
            <groupId>cn.aryee.foundation</groupId>
            <artifactId>commons-spring</artifactId>
        </dependency>

        <!-- 通信模块 -->
        <dependency>
            <groupId>cn.aryee.foundation</groupId>
            <artifactId>transport-spring-boot-starter</artifactId>
        </dependency>

        <!-- 网关模块 -->
        <dependency>
            <groupId>cn.aryee.foundation</groupId>
            <artifactId>gateway-spring-boot-starter</artifactId>
        </dependency>

        <!-- 监控模块 -->
        <dependency>
            <groupId>cn.aryee.foundation</groupId>
            <artifactId>monitoring-spring-boot-starter</artifactId>
        </dependency>

        <!-- 存储模块 -->
        <dependency>
            <groupId>cn.aryee.foundation</groupId>
            <artifactId>storage-spring-boot-starter</artifactId>
        </dependency>

        <!-- 安全模块 -->
        <dependency>
            <groupId>cn.aryee.foundation</groupId>
            <artifactId>security-spring-boot-starter</artifactId>
        </dependency>

        <!-- 事件模块 -->
        <dependency>
            <groupId>cn.aryee.foundation</groupId>
            <artifactId>event-spring-boot-starter</artifactId>
        </dependency>

        <!-- 调度模块 -->
        <dependency>
            <groupId>cn.aryee.foundation</groupId>
            <artifactId>scheduler-spring-boot-starter</artifactId>
        </dependency>

        <!-- ========== Spring Boot & 第三方 ========== -->

        <!-- Spring Boot Web (Blocking/WebMVC 场景) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Spring Boot Actuator（健康检查、指标暴露） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- Spring Boot Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>21</release>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${spring.boot.version}</version>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>